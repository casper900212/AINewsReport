version: '3.8'

services:
  # app:
  #   build: .
  #   container_name: news-monthly-report-app
  #   volumes:
  #     - .:/app
  #   working_dir: /app
  #   command: bash -c "npm install && npm run dev"
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=development
  #     - QDRANT_HOST=qdrant
  #     - QDRANT_PORT=6333
  #   depends_on:
  #     - qdrant

  qdrant:
    image: qdrant/qdrant
    container_name: qdrant-vector-db
    ports:
      - "6333:6333"
    volumes:
      - ./qdrant_data:/qdrant/storage

